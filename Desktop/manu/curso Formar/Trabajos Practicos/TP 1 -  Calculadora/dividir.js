function dividir(a,b) {
    if(b === 0 ) {

        return `No se puede Dividir por 0 !!!`;

    } else if (a === 0 ) {

        return `No se puede Dividir por 0 !!!`;

    } return a / b ;

}
module.exports = dividir;